# 팀 내 모의경기 가이드 (각자 PC에서 실행)

## 0. 구조 이해

```
  [PC-A]  DogFightViewer.exe  ← 교전 서버 + 뷰어. 한 대만 실행한다.
             ↑ UDP 9999 ↑
  [PC-A] my_submission.py   [PC-B] my_submission.py
   (A팀 AI = 접속기)          (B팀 AI = 접속기)
```

- **서버는 한 대에서만** 실행한다. 그 PC가 두 기체의 물리·데미지 연산을 다 한다.
- **각 팀은 자기 PC에서 자기 접속기**를 실행해 그 서버에 붙는다.
- 서버를 돌리는 PC도 자기 접속기를 따로 실행해야 한다(서버 ≠ 참가자).

---

## 1. 서버 담당 PC에서 할 일

### (1) 서버 실행
```
<자기 경로>\BattleViewer\BattleServer_V0.2\DogFightViewer.exe
```
사양이 부족하면 `BattleServer_Low_V0.2\DogFightViewer.exe` 를 쓴다.

### (2) 자기 IP 확인 → 상대에게 알려주기
명령 프롬프트에서:
```
ipconfig
```
**무선 LAN 어댑터의 IPv4 주소**를 찾는다. 핫스팟이면 보통 `172.20.10.x` 또는 `192.168.x.x`.

### (3) 방화벽 허용
연결이 안 되면 대개 방화벽이다. 관리자 권한 PowerShell에서:
```
New-NetFirewallRule -DisplayName "DogFight 9999" -Direction Inbound -Protocol UDP -LocalPort 9999 -Action Allow
```
또는 실행 시 뜨는 Windows 방화벽 팝업에서 **개인 네트워크 허용**을 체크한다.

---

## 2. 양쪽 PC 모두 할 일 — 설정 3군데

파일: `<자기 경로>\강화학습환경\Release\student\my_submission.py`

| 줄 | 항목 | 바꿀 값 |
|---|---|---|
| 61 | `TEAM_NAME` | **자기 팀 이름.** 두 팀이 서로 달라야 한다 |
| 62 | `SERVER_IP` | **서버 담당 PC의 IPv4** (1-(2)에서 확인한 값) |
| 85 | `AI_TYPE` | BT 기체면 `AIType.RuleBased` / RL이면 `AIType.ReinforcementLearning` |

`SERVER_PORT = 9999`는 그대로 둔다.

### 자기 기체 지정 (BT를 쓰는 경우)
```python
MODE = "bt"
BT_DLL      = "AIP_final.dll"    # 자기 DLL 파일명
BT_RULE_XML = "Rule_v32.xml"     # 자기 XML 파일명
ACTION_REPEAT = 6                # 대회 규격. 건드리지 말 것
```
- **DLL과 XML은 한 세트다.** DLL 안에 XML 파일명이 박혀 있으므로 짝을 맞춰야 한다.
- `activate_rule_xml`이 실행 시 `BT_RULE_XML`을 **`Rule_forTraining.xml`이라는 고정 이름으로 복사**해서 DLL에 먹인다. 그래서 DLL은 `Rule_forTraining.xml`을 읽도록 빌드돼 있어야 한다.
- ⚠ **한 PC에서 두 기체를 동시에 돌리면 `Rule_forTraining.xml`을 서로 덮어써서 충돌한다.**
  각자 PC에서 하나씩 돌리면 문제없다.

---

## 3. 실행 (양쪽 각자)

```
cd <자기 경로>\강화학습환경\Release
<자기 python 경로> student\my_submission.py
```

※ python은 이 프로젝트 전용 환경을 써야 한다(그냥 `python`이면 `pymap3d` 없음 오류).
   예: `C:\Users\<사용자>\anaconda3\envs\aip\python.exe`

### 정상 접속 확인
콘솔에 이런 줄이 뜨면 성공:
```
=== <팀이름> 경진대회 클라이언트 시작 ===
서버: <IP>:9999
모드: bt
BT DLL/XML: AIP_final.dll / Rule_v32.xml
[bt_rule_manager] activated Rule XML: ... -> Rule_forTraining.xml
```

---

## 4. 안 될 때 점검 순서

| 증상 | 확인할 것 |
|---|---|
| 서버에 안 붙음 | ①서버 PC IP가 맞나 ②방화벽 UDP 9999 ③같은 핫스팟인가 |
| `ModuleNotFoundError: pymap3d` | python 경로가 전용 환경(aip)인지 |
| BT 초기화 실패 | `BT_RULE_XML` 파일이 `Release/` 안에 실제로 있는지 |
| 기체가 안 움직임 | `MODE`가 `"bt"`인지, `BT_DLL` 파일명이 맞는지 |
| 한쪽만 반응 | 두 팀의 `TEAM_NAME`이 서로 다른지 |

---

## 5. 경기 규칙 (대회 기준)

- 라운드 **200초**. 만료 시 **잔여 HP 비율이 높은 쪽 승**
- 고도 **약 300m 이하 = 즉시 패배**
- 사격 판정은 **시간에 따라 3단계로 완화**된다:
  - 0~100초: LOS < 1°, 500~3000ft, 계수 1.0
  - 100~150초: LOS < 2°, 500~3500ft, 계수 0.3
  - 150~200초: LOS < 3°, 500~4000ft, 계수 0.1
  - 좁은 콘 안에 있으면 그 높은 계수를 그대로 받는다
- 양 기체 모두 30초+ 회피만 하고 이후 30초 내 교전 미성립 시 **무승부**
