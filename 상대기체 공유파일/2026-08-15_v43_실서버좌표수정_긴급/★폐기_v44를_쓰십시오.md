# ⚠ 이 패키지는 폐기됐습니다 — `2026-08-15_v44_좌표버그_두곳수정/` 을 쓰십시오

v43은 `Step()`의 좌표 버그만 고쳤습니다. **같은 버그가 `GetStick()`
(`LibMain.cpp:297`)에도 있었고 v43은 그걸 안 고쳤습니다.**

`GetStick`을 부르는 하네스에서는 **v43도 그대로 눈이 멉니다.**

불변성 시험(상대 기하 고정, 절대 위치만 25km 이동):

```
AIP_ryujan_v43.dll   A(원점) (+1.0000,-0.0000,-1.0000)   B(25km) (+1.0000,-1.0000,-1.0000)   다름 -> 변환함
AIP_ryujan_v44.dll   A(원점) (+0.0000,-0.0000,+0.0000)   B(25km) (+0.0000,-0.0000,+0.0000)   같음 OK
```

v44는 변환을 `ChangeData()` 한 곳으로 모아 두 곳을 동시에 해결했습니다.
