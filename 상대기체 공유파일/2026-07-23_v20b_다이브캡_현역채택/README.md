# v20b — 다이브 클램프 캡 650m (현역 채택)

## 변경 (v18 대비 단 한 줄)
```cpp
double diveSlope = dist * 0.5;
if (diveSlope > 650.0) diveSlope = 650.0;   // v20b: 원거리 다이브 깔때기 억제
```
사거리(<914m)에선 dist*0.5 < 457m라 **v18의 조준 성과에 영향 없음(연속)**.
원거리에서만 VP 강하 기울기를 죄어 "급기동 상대의 다이브 리드를 쫓는 깊은 다이브"를 완화.

## 판정 근거 — 15시드 6/6 (제출조건 에뮬레이션) 정면 비교
| | v18 | **v20b** |
|---|---|---|
| 완전 격추 | 2 (seed 2,9) | **3 (seed 2,3,5)** |
| 데미지 준 판 | 4 | **6** |
| 총 데미지 | 3.216 | **4.594** |
| **패배** | **2** (seed5 지면추락사 / seed13 피격추) | **0** |
| 받은 데미지 | 1.785 | **0.000** |

**결정타는 점수가 아니라 패배 모드 제거다:**
- v18 seed5 = `ownship altitude below min` — **다이브 깔때기가 실제로 우리를 죽였다**
  (v20b에서 같은 시드는 오히려 완전 격추 승리).
- v18 seed13 = `ownship destroyed` — "무피격" 프로필도 15시드에선 깨졌다.
- **5시드 배치는 이 두 패배 모드를 전혀 못 봤다. 15시드 이상이 필요하다는 실증.**

## 과정에서 폐기된 것들 (같은 날, 각각 메커니즘으로 반증)
- **v19 에너지 요격**: 이 시뮬은 상승이 속도를 안 깎아 전제 자체가 거짓.
- **v20(a) omega 연동 리드 축소**: 3초 리드가 머지에서 코너를 가로질러 각도전을
  이기는 우리 무기였음이 실측됨(six 장악 76%→30% 붕괴). **리드는 만지지 말 것.**

## 남은 한계 (정직하게)
- 다이브 캡은 기울기 제한일 뿐 랫칫 하강 자체를 못 막는다(seed0에서 최저 524m 실측,
  ClimbOut 1800m가 구조). 절대고도 안전망 보강 여지 있음.
- 교착(사거리 밖 dead-six 고착) 문제는 여전히 미해결 — v19/v20a 둘 다 실패.
  무득점 무승부 9/15판이 그 결과. 다음 접근은 상태분기(머지 전용 로직) 또는
  Phase 2(다양한 상대)로 교착이 권정환 전용 문제인지 먼저 확인.
- 리플레이: seed5 격추판 = `2026_7_23_6_42_15` (시드0~5 순서 재현, bit-identical 확인)
